{
  "name": "o2n-auth-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "o2nのCLI/MCPサーバーがNotion OAuth(public integration)のclient_secretを持たずに済むよう、コード交換だけを代行するCloudflare Worker",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260719.1",
    "typescript": "^7.0.2",
    "wrangler": "^4.112.0"
  }
}
